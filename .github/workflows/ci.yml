name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      # The play window (winit + softbuffer) needs X11/Wayland headers to build
      # on Linux. Nothing in kessel-vm does — see the `headless` job.
      - name: Install window-system headers
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libxkbcommon-dev libwayland-dev libx11-dev \
                                  libxcb1-dev libxrandr-dev libxi-dev pkg-config

      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            crates/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('crates/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Test
        working-directory: crates
        run: cargo test

  # `kessel mcp` must build and pass with no window system at all — that is the
  # configuration it runs in on a server or in a container, and it is easy to
  # break by reaching for a windowing type outside the `play` feature.
  headless:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Build and test without the player
        working-directory: crates
        run: |
          cargo build --no-default-features
          cargo test -p kessel-vm
          cargo test -p kessel --no-default-features

  fmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - working-directory: crates
        run: cargo fmt --check
