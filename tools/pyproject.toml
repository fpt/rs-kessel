[project]
name = "kessel-tools"
version = "0.1.0"
description = "Asset tools for Kessel: turn generated art into luax sprite declarations"
requires-python = ">=3.12"
dependencies = [
    "pillow>=11.0",
    "numpy>=2.1",
    "httpx>=0.28",
]

[dependency-groups]
dev = []
