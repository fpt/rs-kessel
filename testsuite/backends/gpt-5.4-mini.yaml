# Cloud OpenAI gpt-5.4-mini (Responses API). apiKey falls back to OPENAI_API_KEY.
llm:
  baseURL: "https://api.openai.com/v1"
  model: "gpt-5.4-mini"
  apiKey: ""
  harmonyTemplate: false
  maxTokens: 2048

agent:
  language: "en"
  maxTurns: 50
